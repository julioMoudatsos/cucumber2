/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package testandoapis.app;

import testandoapis.list.LinkedList;

import static testandoapis.utilities.StringUtils.join;
import static testandoapis.utilities.StringUtils.split;
import static testandoapis.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
